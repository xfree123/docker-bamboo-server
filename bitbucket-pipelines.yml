---
# NOTE: This file is generated from `bitbucket-pipelines.yml.j2` via `pipelines-generator.py`:
#
#     python pipelines-generator.py > bitbucket-pipelines.yml

image: atlassian/docker-release-maker:latest

pipelines:

######################################################################
# The release and master pipelines are currently disabled during
# development, as there is an internal release plan. The following
# sections should only be uncommented once that has been deprecated.
######################################################################


  custom:
    ######################################################################
    # New Release: Do full release for each image flavour
    ######################################################################
    new-releases:
      - step:
          name: Check if pipelines config is up-to-date...
          script:
            - python3 pipelines-generator.py > bitbucket-piplines.yml.expected && diff bitbucket-pipelines.yml bitbucket-piplines.yml.expected

      - parallel:
          - step:
              name: Bamboo JDK 11 - Batch 1
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --create --create-eap \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='0' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 2
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --create --create-eap \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='1' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 3
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --create --create-eap \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='2' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 4
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --create --create-eap \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='3' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 5
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --create --create-eap \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='4' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 6
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --create --create-eap \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='5' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 7
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --create --create-eap \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='6' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 8
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --create --create-eap \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='7' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 9
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --create --create-eap \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='8' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 10
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --create --create-eap \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='9' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 11
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --create --create-eap \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='10' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 12
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --create --create-eap \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='11' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 13
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --create --create-eap \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='12' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 14
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --create --create-eap \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='13' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 15
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --create --create-eap \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='14' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push


    ######################################################################
    # Custom: Do full release for each image flavour
    ######################################################################
    jdk11-custom-release:
      - variables:
          - name: BAMBOO_VERSION
          - name: DOCKER_TAG
      - step:
          services:
            - docker
          script:
            - git submodule update --init --recursive
            - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
            - >
              docker build -t atlassian/bamboo:${DOCKER_TAG} \
                --build-arg BASE_IMAGE=adoptopenjdk/openjdk11 \
                --build-arg BAMBOO_VERSION=${BAMBOO_VERSION} .
            - docker push atlassian/bamboo:${DOCKER_TAG}

  branches:
    ######################################################################
    # Master branch: Do full release for each image flavour
    ######################################################################
    master:
      - step:
          name: Check if pipelines config is up-to-date...
          script:
            - python3 pipelines-generator.py > bitbucket-piplines.yml.expected && diff bitbucket-pipelines.yml bitbucket-piplines.yml.expected

      - parallel:
          - step:
              name: Bamboo JDK 11 - Batch 1
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='0' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 2
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='1' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 3
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='2' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 4
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='3' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 5
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='4' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 6
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='5' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 7
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='6' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 8
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='7' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 9
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='8' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 10
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='9' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 11
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='10' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 12
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='11' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 13
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='12' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 14
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='13' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

          - step:
              name: Bamboo JDK 11 - Batch 15
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='8' \
                    --end-version='9' \
                    --default-release \
                    --dockerfile-buildargs='BASE_IMAGE=adoptopenjdk/openjdk11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='14' \
                    --jobs-total='15' \
                    --docker-repos='atlassian/bamboo,atlassian/bamboo-server' \
                    --push

      - step:
          name: Update README
          image: python:3.7-alpine3.9
          script:
            - apk add --no-cache git
            - git submodule update --init --recursive
            - pip install -q requests
            - export DOCKER_REPO='atlassian/bamboo'
            - python shared-components/image/push-readme.py
            - export DOCKER_REPO='atlassian/bamboo-server'
            - python shared-components/image/push-readme.py

    ######################################################################
    # All other branches & PRs; run unit tests & functional tests
    # against latest app version
    ######################################################################
    '**':
      - step:
          name: Check if pipelines config is up-to-date...
          script:
            - python3 pipelines-generator.py > bitbucket-piplines.yml.expected && diff bitbucket-pipelines.yml bitbucket-piplines.yml.expected

      - parallel:
        - step:
            image: python:3.7-alpine3.9
            services:
              - docker
            script:
              - apk add --no-cache git
              - git submodule update --init --recursive
              - pip install -q -r shared-components/tests/requirements.txt
              - export PYTHONPATH=./shared-components/tests:$PYTHONPATH
              - export DOCKERFILE='Dockerfile'
              - export DOCKERFILE_VERSION_ARG='BAMBOO_VERSION'
              - export MAC_PRODUCT_KEY='bamboo'
              - py.test -v tests/
              - py.test -v shared-components/tests/
        - step:
            services:
              - docker
            script:
              - apk add --no-cache git docker-compose jq curl
              - git submodule update --init --recursive
              - export BAMBOO_VERSION=$(curl -s https://marketplace.atlassian.com/rest/2/products/key/bamboo/versions/latest | jq -r .name)
              - docker build --build-arg BAMBOO_VERSION=${BAMBOO_VERSION} -t test-image .
              - export IS_RELEASE=false
              - /usr/src/app/post_build.sh test-image $IS_RELEASE

definitions:
  services:
    docker:
      memory: 4096

options:
  size: 2x
  max-time: 120
