---
# NOTE: This file is generated from `bitbucket-pipelines.yml.j2` via `pipelines-generator.py`:
#
#     python pipelines-generator.py > bitbucket-pipelines.yml

image: docker-public.packages.atlassian.com/atlassian/docker-release-maker:latest

pipelines:

######################################################################
# The release and master pipelines are currently disabled during
# development, as there is an internal release plan. The following
# sections should only be uncommented once that has been deprecated.
######################################################################


  custom:
    ######################################################################
    # New Release: Do full release for each image flavour
    ######################################################################
    new-releases:
      - step:
          name: Check if pipelines config is up-to-date...
          script:
            - python3 pipelines-generator.py > bitbucket-piplines.yml.expected && diff bitbucket-pipelines.yml bitbucket-piplines.yml.expected

      - parallel:
          - step:
              name: Bamboo JDK 17 - Batch 1
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --create --create-eap \
                    --start-version='9.4' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk17,ubuntu' \
                    --job-offset='0' \
                    --jobs-total='8' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17-ubi - Batch 1
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --create --create-eap \
                    --start-version='9.4' \
                    --dockerfile='Dockerfile.ubi' \
                    --dockerfile-buildargs='BASE_IMAGE=registry.access.redhat.com/ubi9/openjdk-17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='ubi9,ubi9-jdk17' \
                    --job-offset='0' \
                    --jobs-total='8' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 1
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --create --create-eap \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='0' \
                    --jobs-total='8' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64


    ######################################################################
    # Custom: Do full release for each image flavour
    ######################################################################
    jdk11-custom-release:
      - variables:
          - name: BAMBOO_VERSION
          - name: DOCKER_TAG
      - step:
          services:
            - docker
          script:
            - git submodule update --init --recursive
            - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
            - >
              docker build -t ${DOCKER_REGISTRY}/atlassian/bamboo:${DOCKER_TAG} \
                --build-arg BASE_IMAGE=eclipse-temurin:11 \
                --build-arg BAMBOO_VERSION=${BAMBOO_VERSION} .
            - docker push ${DOCKER_REGISTRY}/atlassian/bamboo:${DOCKER_TAG}

  branches:
    ######################################################################
    # Master branch: Do full release for each image flavour
    ######################################################################
    master:
      - step:
          name: Check if pipelines config is up-to-date...
          script:
            - python3 pipelines-generator.py > bitbucket-piplines.yml.expected && diff bitbucket-pipelines.yml bitbucket-piplines.yml.expected

      - parallel:
          - step:
              name: Bamboo JDK 17 - Batch 1
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk17,ubuntu' \
                    --job-offset='0' \
                    --jobs-total='16' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17 - Batch 2
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk17,ubuntu' \
                    --job-offset='1' \
                    --jobs-total='16' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17 - Batch 3
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk17,ubuntu' \
                    --job-offset='2' \
                    --jobs-total='16' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17 - Batch 4
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk17,ubuntu' \
                    --job-offset='3' \
                    --jobs-total='16' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17 - Batch 5
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk17,ubuntu' \
                    --job-offset='4' \
                    --jobs-total='16' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17 - Batch 6
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk17,ubuntu' \
                    --job-offset='5' \
                    --jobs-total='16' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17 - Batch 7
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk17,ubuntu' \
                    --job-offset='6' \
                    --jobs-total='16' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17 - Batch 8
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk17,ubuntu' \
                    --job-offset='7' \
                    --jobs-total='16' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17 - Batch 9
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk17,ubuntu' \
                    --job-offset='8' \
                    --jobs-total='16' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17 - Batch 10
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk17,ubuntu' \
                    --job-offset='9' \
                    --jobs-total='16' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17 - Batch 11
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk17,ubuntu' \
                    --job-offset='10' \
                    --jobs-total='16' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17 - Batch 12
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk17,ubuntu' \
                    --job-offset='11' \
                    --jobs-total='16' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17 - Batch 13
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk17,ubuntu' \
                    --job-offset='12' \
                    --jobs-total='16' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17 - Batch 14
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk17,ubuntu' \
                    --job-offset='13' \
                    --jobs-total='16' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17 - Batch 15
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk17,ubuntu' \
                    --job-offset='14' \
                    --jobs-total='16' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17 - Batch 16
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --default-release \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk17,ubuntu' \
                    --job-offset='15' \
                    --jobs-total='16' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17-ubi - Batch 1
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --dockerfile='Dockerfile.ubi' \
                    --dockerfile-buildargs='BASE_IMAGE=registry.access.redhat.com/ubi9/openjdk-17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='ubi9,ubi9-jdk17' \
                    --job-offset='0' \
                    --jobs-total='8' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17-ubi - Batch 2
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --dockerfile='Dockerfile.ubi' \
                    --dockerfile-buildargs='BASE_IMAGE=registry.access.redhat.com/ubi9/openjdk-17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='ubi9,ubi9-jdk17' \
                    --job-offset='1' \
                    --jobs-total='8' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17-ubi - Batch 3
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --dockerfile='Dockerfile.ubi' \
                    --dockerfile-buildargs='BASE_IMAGE=registry.access.redhat.com/ubi9/openjdk-17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='ubi9,ubi9-jdk17' \
                    --job-offset='2' \
                    --jobs-total='8' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17-ubi - Batch 4
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --dockerfile='Dockerfile.ubi' \
                    --dockerfile-buildargs='BASE_IMAGE=registry.access.redhat.com/ubi9/openjdk-17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='ubi9,ubi9-jdk17' \
                    --job-offset='3' \
                    --jobs-total='8' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17-ubi - Batch 5
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --dockerfile='Dockerfile.ubi' \
                    --dockerfile-buildargs='BASE_IMAGE=registry.access.redhat.com/ubi9/openjdk-17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='ubi9,ubi9-jdk17' \
                    --job-offset='4' \
                    --jobs-total='8' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17-ubi - Batch 6
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --dockerfile='Dockerfile.ubi' \
                    --dockerfile-buildargs='BASE_IMAGE=registry.access.redhat.com/ubi9/openjdk-17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='ubi9,ubi9-jdk17' \
                    --job-offset='5' \
                    --jobs-total='8' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17-ubi - Batch 7
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --dockerfile='Dockerfile.ubi' \
                    --dockerfile-buildargs='BASE_IMAGE=registry.access.redhat.com/ubi9/openjdk-17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='ubi9,ubi9-jdk17' \
                    --job-offset='6' \
                    --jobs-total='8' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 17-ubi - Batch 8
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9.4' \
                    --dockerfile='Dockerfile.ubi' \
                    --dockerfile-buildargs='BASE_IMAGE=registry.access.redhat.com/ubi9/openjdk-17' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='ubi9,ubi9-jdk17' \
                    --job-offset='7' \
                    --jobs-total='8' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 1
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='0' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 2
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='1' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 3
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='2' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 4
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='3' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 5
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='4' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 6
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='5' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 7
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='6' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 8
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='7' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 9
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='8' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 10
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='9' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 11
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='10' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 12
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='11' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 13
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='12' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 14
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='13' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 15
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='14' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 16
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='15' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 17
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='16' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 18
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='17' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 19
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='18' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 20
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='19' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 21
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='20' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 22
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='21' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 23
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='22' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64

          - step:
              name: Bamboo JDK 11 - Batch 24
              services:
                - docker
              script:
                - git submodule update --init --recursive
                - echo ${DOCKER_BOT_PASSWORD} | docker login ${DOCKER_REGISTRY} --username ${DOCKER_BOT_USERNAME} --password-stdin
                - docker buildx create --name container --driver docker-container --use
                - docker buildx ls
                - >
                  python /usr/src/app/make-releases.py \
                    --update \
                    --start-version='9' \
                    --end-version='10' \
                    --dockerfile='Dockerfile' \
                    --dockerfile-buildargs='BASE_IMAGE=eclipse-temurin:11' \
                    --dockerfile-version-arg='BAMBOO_VERSION' \
                    --mac-product-key='bamboo' \
                    --tag-suffixes='jdk11,ubuntu' \
                    --job-offset='23' \
                    --jobs-total='24' \
                    --docker-repos='atlassian/bamboo' \
                    --push \
                    --platforms=linux/amd64,linux/arm64


    ######################################################################
    # All other branches & PRs; run unit tests & functional tests
    # against latest app version
    ######################################################################
    '**':
      - step:
          name: Check if pipelines config is up-to-date...
          script:
            - python3 pipelines-generator.py > bitbucket-piplines.yml.expected && diff bitbucket-pipelines.yml bitbucket-piplines.yml.expected

      - parallel:
        - step:
            name: Run unit tests
            image: python:3.7-alpine3.9
            services:
              - docker
            script:
              - apk add --no-cache git
              - git submodule update --init --recursive
              - pip install -q -r shared-components/tests/requirements.txt
              - export PYTHONPATH=./shared-components/tests:$PYTHONPATH
              - export DOCKERFILE='Dockerfile'
              - export DOCKERFILE_VERSION_ARG='BAMBOO_VERSION'
              - export MAC_PRODUCT_KEY='bamboo'
              - py.test -v tests/
              - py.test -v shared-components/tests/
        - step:
            name: Run unit tests - UBI
            image: python:3.7-alpine3.9
            services:
              - docker
            script:
              - apk add --no-cache git
              - git submodule update --init --recursive
              - pip install -q -r shared-components/tests/requirements.txt
              - export PYTHONPATH=./shared-components/tests:$PYTHONPATH
              - export DOCKERFILE='Dockerfile.ubi'
              - export DOCKERFILE_VERSION_ARG='BAMBOO_VERSION'
              - export MAC_PRODUCT_KEY='bamboo'
              - py.test -v tests/
        - step:
            name: Run functional tests
            services:
              - docker
            script:
              - apk add --no-cache git docker-compose jq curl
              - git submodule update --init --recursive
              - export BAMBOO_VERSION=$(curl -s https://marketplace.atlassian.com/rest/2/products/key/bamboo/versions | jq -r '._embedded.versions | .[0].name')
              - docker build --build-arg BAMBOO_VERSION=${BAMBOO_VERSION} -t test-image .
              - export IS_RELEASE=false
              - /usr/src/app/post_build.sh test-image $IS_RELEASE
        - step:
            name: Run functional tests - UBI
            services:
              - docker
            script:
              - apk add --no-cache git docker-compose jq curl
              - git submodule update --init --recursive
              - export BAMBOO_VERSION=$(curl -s https://marketplace.atlassian.com/rest/2/products/key/bamboo/versions | jq -r '._embedded.versions | .[0].name')
              - docker build --build-arg BAMBOO_VERSION=${BAMBOO_VERSION} -t test-image -f Dockerfile.ubi .
              - export IS_RELEASE=false
              - /usr/src/app/post_build.sh test-image $IS_RELEASE

definitions:
  services:
    docker:
      memory: 4096

options:
  size: 2x
  max-time: 120
